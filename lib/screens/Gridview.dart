import 'package:flutter/material.dart';

class TransactionGridsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Grid view screen')),
      body: Center(child: Text('Grid view')),
    );
  }
}